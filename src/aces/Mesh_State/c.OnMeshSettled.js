export const config = {
  listName: "On settled",
  displayText: "On mesh settled",
  description: "Triggered when mesh spring animation settles.",
  isTrigger: true,
  params: [],
};

export default function () {
  return true;
}
