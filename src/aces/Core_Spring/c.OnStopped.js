export const config = {
  isDeprecated: true,
  listName: "On stopped",
  displayText: "(DEPRECATED) On stopped",
  description: "Deprecated. Use 'On spring stopped' in the Multi Spring category instead.",
  isTrigger: true,
  params: [],
};

export default function () {
  return true;
}
